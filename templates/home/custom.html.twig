{% extends('base.html.twig') %}

{% block body %}

    <div class="container">
        <div class="example-wrapper">
            <div class="card" style="width: 18rem;">
                <img  src="{{ '/uploads/' ~ post.image }}" class="card-img-top" alt="...">
                <div class="card-body">
                    <h1 class="card-title" style="color: #0f74a8">{{ post.title }}</h1>
                    <p class="card-text">{{ post.description | raw }}</p>
                    {#                <a href="#" class="btn btn-primary">Go somewhere</a>#}
                </div>
            </div>
            <hr>
            <a href="/">Go Back</a>
            <hr>
        </div>
    </div>
{% endblock %}
